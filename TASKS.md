# TASKS — タスクリスト

## フェーズ1: フロントモック（front/ のみ）

- [x] SPEC.md / DESIGN.md / AGENTS.md / TASKS.md 作成
- [x] Vite + React + TypeScript プロジェクト作成（front/）
- [x] ゲームコアデータモデル定義（types.ts）
- [x] 盤面シミュレータ実装（simulator.ts、純粋関数）
- [x] ブロックインタプリタ実装（interpreter.ts: ブロック列 → トレース）
- [x] ステージデータ作成（初級5ステージ、ハードコード）
- [x] Board コンポーネント（グリッド描画 + トレース再生アニメーション）
- [x] BlockEditor コンポーネント（パレット / ワークスペース / repeat ネスト）
- [x] PlayScreen（実行・停止・リセット・速度調整・クリア判定・星評価）
- [x] TitleScreen / StageSelect（モード選択、クリア状況表示）
- [x] CodeEditor プレースホルダ（上級モードの UI モック）
- [x] クリア状況の localStorage 保存
- [x] 動作確認（全ブロックステージが★3でクリア可能なことをインタプリタで検証済み）

## フェーズ2: バックエンド（back/、Rust）

- [x] axum プロジェクトセットアップ
- [x] ステージ配信 API（GET /api/stages, /api/stages/:id）
- [x] フロントのステージ取得を API に差し替え（API 停止時は同梱データにフォールバック）
- [x] 進捗保存 API（GET/POST /api/progress、SQLite）
- [x] Cコード実行サンドボックス方式の決定（WASM 案を検証 → この環境に clang wasm32 / wasmtime が無いため、gcc ネイティブ + setrlimit + タイムアウト方式を採用。WASM 化は本番強化パスとして DESIGN.md に記録）
- [x] game.h API 実装 + トレース生成（back/runtime/gamelib.c）
- [x] POST /api/run/c 実装（コンパイルエラー整形・無限ループ停止含む）
- [x] 上級モードのフロント統合（エディタ → 実行 → トレース再生。初級と再生コード共通）

## フェーズ3: 問題の作成

- [x] しょきゅう問題を 200 問作成する（scripts/gen-stages.ts で生成し、全問クリア可能なことを機械検証。テストでも常時検証）
- [x] 上級問題を 100 問題作成する（同上。全問について右手法で解けることを検証）

## フェーズ4: 磨き込み

- [x] ブロックのドラッグ&ドロップ対応（クリック追加も併用可）
- [x] 条件分岐ブロック（もし まえがかべ なら）+ ずっとくりかえすブロック
- [x] 上級ステージ追加（センサー API が必要な迷路。初級にもセンサーめいろ45問）
- [x] 効果音・BGM（WebAudio 合成、音声ファイル不使用。トグルで ON/OFF）
- [x] コードエディタの本格化（CodeMirror + C シンタックスハイライト）
- [x] E2E テスト / インタプリタの単体テスト整備（vitest 313件: インタプリタ単体・全300ステージ検証・画面フロー。バックエンド API は curl で疎通検証済み）
- [x] デプロイ構成（front: nginx 静的配信+APIプロキシ、back: コンテナ。docker-compose.yml）
- [x] しょきゅう問題がものすごく単調でつまらない問題ばかりなので、問題の出し方の順番だったり、ほとんど変化のない問題を削除して新たな問題を作るなどの改善
      （単純形の量産を削減: まっすぐ30→6問など。新形状4種を追加: うずまき/へびみち/じゆうなみち/おおべや。
      出題順は最短手数ベースの難易度順にし、同じ形状が続かないよう混合。同一盤面は生成時に除去。
      センサーめいろは80問目以降に通常問題と交互に出題）
- [x] 私がバカじゃなければ多分上級問題が解けないのでテストして修正を
      （実サンドボックスで全100問に右手法・左手法のCコードを実行して検証。
      修正前は左手法が17問ステップ上限で失敗 → maxSteps を両手法の実測×2+100に修正し、
      右手法・左手法とも100/100クリアを確認。初級センサー問題も両手法で解けることをテストで常時検証。
      デフォルトテンプレートは意図的に迷路が解けない単純作戦のため、その旨と改造の方向性をコメントで明示）
- [x] 上級問題が全部迷路問題のせいですべての問題で全く同じプログラムを使っても正解できてしまうので、問題のバリエーションを増やす(今思いついている問題だけでもソートとかあるから Fable ならもっといろんな問題を作れるはず)
      （上級100問を「ナビ60問 + 配列パズル40問」に再構成。配列パズルは swap 操作の API
      (array_length / get_value / swap_values) を game.h に追加し、課題5種:
      昇順ソート14・降順ソート8・逆順8・最大値を最後へ5・最小値を先頭へ5。
      星は交換回数で評価(バブルソート=★2、選択ソート等=★3)。棒グラフ表示の
      ArrayBoard で swap を再生。ナビ側も迷路一色をやめ、まっすぐ/ジグザグ/うずまき/
      へびみち/一本道の地形30問を混合。全問を実サンドボックスで標準解法により検証済み）
- [x] しょきゅう問題のバリエーションを増やすのと問題の見直し
      （新形状2種を追加: わかれみち(いきどまりの枝で迷わせる一本道)15問・
      どうくつ(セルオートマトン地形)12問。既存形状の比率も見直し(めいろ45→38等)。
      全200問の解在性・センサー問題の左右手法可解性はテストで常時検証）

## フェーズ5: もっと面白く（Fable 5 リード + Sonnet 5 チームでブレスト→実装）

Fable 5 リードのブレストで出た案（fun ÷ cost 順）。実装しながら随時チェックする。

- [x] 盤面の演出強化（なめらかな移動アニメ、ぶつかり時シェイク、ゴール時の紙吹雪）
      （Board.tsxのプレイヤーを盤面上の絶対配置オーバーレイに変更しCSS transitionで
      セル間をなめらかに移動。クラッシュ時はshakeアニメ、ゴール時は絵文字紙吹雪を
      バースト表示。Playwrightで実ブラウザ動作確認済み、vitest 355件も全パス）
- [x] きせかえキャラ（絵文字スキン、★の累計で新スキン解放）
      （game/skins.ts に6種のスキン定義(やじるし/ねこ/ロボット/かえる/ドラゴン/ロケット、
      ★10〜150で解放)。SkinPickerをタイトル画面に設置、選択状態はlocalStorage永続化。
      Board.tsxはプレイヤーを絵文字にすると向きに応じてCSS回転。Playwrightで解放前後の
      表示・選択・盤面反映を実ブラウザで確認、vitest 355件も全パス）
- [x] 実行中ブロックのハイライト表示（BlockEditor で今動いているブロックが光る）
      （interpreter.ts の run() が trace と並行して blockIds(各イベントを起こした
      ブロックid)を RunResult に追加。PlayScreen が再生ステップごとに activeBlockId
      を更新しBlockEditorへ渡す。該当ブロックは黄色グロー+パルスアニメ、祖先の
      repeat/ifWallも薄く縁取り表示。Playwrightで1問目→2問目へハイライトが
      正しく移動することを確認、vitest 355件も全パス）
- [x] キャラのふきだしリアクション（ぶつかった/ゴールでひとことセリフ、ランダム）
      （Board.tsxにcrashed/goaledの立ち上がりでランダムな一言を選ぶふきだしを追加。
      「いたた!」「やったー!」等をプレイヤーの上に表示、ポップインアニメ付き。
      ゴール側はPlaywrightで表示を確認、vitest 355件も全パス）
- [x] ステップ実行ボタン（1コマずつ進める）
      （PlayScreenにトレース反映処理をapplyEvent()として共通化し、自動再生と
      手動ステップの両方で共有。「⏭ ステップ」ボタンでトレースを1件ずつ適用し、
      押すたびに「⏭ つぎへ」に変化。ステップ中は編集・通常実行を無効化。
      初級ブロック・上級Cコード両モードで対応。Playwrightで1手ずつゴールまで
      進むことを確認、vitest 355件も全パス）
- [x] 編集中のブロック数と★ボーダーのライブ表示
      （PlayScreenのBlockEditor上に「つかったブロック: Nこ（★3はXこ・★2はYこまで）」を
      常時表示、ペースに応じて数字を緑/黄/赤に色分け。Playwrightで表示を確認、
      vitest 355件も全パス）
### 6項目完了時点でFableに再レビューしてもらい優先順位を更新（下記の順で実装）

- [x] クリアダイアログの★演出強化（1個ずつ出現+ファンファーレ）
      （goalイベント発生から700ms待ってからclear-overlayを表示するよう修正し、
      紙吹雪・ふきだしがすぐ隠れてしまう問題を解消。★は1つずつポップイン
      アニメ+「ぽろん」音(sound.star())で出現。App.test.tsxのアサーションも
      新しいDOM構造に合わせて更新。Playwrightで演出順序を確認、vitest 355件も全パス）
- [x] ステージセレクトのワールド分け（既存の20件ページングをそのままテーマ分けに流用）
      （くさはら/どうくつ/うみ等10種のワールドをページごとに割り当て、背景色と
      「🌱 くさはらワールド（1/10）」のようなラベルを表示。ついでにページ復元も修正）
- [x] じっせき（バッジ）システム（skins.tsと同じ構造で実装、トースト表示+ギャラリー）
      （game/achievements.tsに9種のバッジ(はじめてクリア〜パーフェクト)を定義し、
      progress情報から達成判定。新規解放はApp.tsxでトースト表示+
      localStorageに既読を記録。タイトル画面にAchievementGallery(未解放は🔒？？？)
      を設置。Playwrightでトースト表示とギャラリー反映を確認、vitest 355件も全パス）
- [x] きょうのチャレンジ（日替わりステージ、ストリークカウンター）
      （game/dailyChallenge.tsで日付から決定的に全300ステージ(初級+上級)から1問選択。
      クリアするとlocalStorageにストリーク日数を記録(前日クリア済みなら+1、
      間が空くと1にリセット)。タイトル画面にDailyChallengeCardを設置し、
      クリア済みならチェックマーク・未クリアならストリーク日数を表示。
      経由時は「もどる」でステージ選択ではなくタイトルへ戻るよう分岐。
      Playwrightで選択→プレイ→タイトルへの復帰を確認、vitest 355件も全パス）
- [ ] 集める要素（宝石マス）: 今回のセッションでは見送り
      （TraceEvent拡張がtypes.ts/trace.rs/gamelib.c/simulator.ts/gen-stages.tsの
      横断変更になり、既存355テストとサンドボックスへの影響が大きいため、
      独立タスクとして別途着手することをFableと合意）
- [x] ~~ベストプレイのゴースト再生~~ → 見送り
      （ステップ実行ボタンで「ゆっくり見る」価値は代替済み、300ステージ分の
      ベストトレース保存はコストに見合わないとFableと合意し取りやめ）

### ついでに直す不具合（Fableのレビューで発見）

- [x] handleStep の二重発火防止（Cコードのコンパイル中に連打すると2重にセッションが走る）
      （stepStartingRefでコンパイル待ち中の二重セッション開始をガード）
- [x] StageSelect がステージ一覧から戻ると常に1ページ目に戻ってしまう（ワールド分けと合わせて対応）
      （pageの初期値を最初の未クリアステージのページからlazy初期化するよう修正）

## フェーズ6: もっと面白く その2（フェーズ5完了後、Fableに再ブレストしてもらった第2弾）

Fable曰く「元のバックログは尽きたが、既存機能の伸びしろと新規の軽量ネタ」を
fun÷cost順に提案。この順で実装する。

- [ ] スキン別セリフ（きせかえキャラごとにクラッシュ/ゴールのセリフを変える）
- [ ] ストリークバッジ + ストリーク実績（3日/7日れんぞくバッジ、カードに🔥強調）
- [ ] こうどうバッジ（ノーミスクリア・いちはつクリア等、PlayScreenに小さいカウンタ追加）
- [ ] ワールド制覇の見える化（ワールドごとの進捗バー、20問全クリアで王冠）
- [ ] あしあと表示（プレイ中に通過マスへフェードするあしあとを重ねる）
- [ ] スキン2種追加（★200/250でユニコーン・エイリアンなど解放）
- [ ] じこベスト記録（ステージごとの最小ブロック数/ステップ数をlocalStorageに保存し更新演出）
- [ ] 集める要素（宝石マス）: 初級(ブロック)モード限定でスコープを絞り再挑戦
      （TraceEventにgemイベントを追加。trace.rs側は未使用のミラー型を追加するのみで
      C側サンドボックス/gamelib.cには手を入れない。最後に着手する）
